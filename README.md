# JLPT N2 Study Planner (React + Material UI) builded by using Copilot AI

## Fixes included
- ✅ DataGrid `valueFormatter` updated for MUI X v7 (it now receives the raw `value`, which can be `null`).
- ✅ DataGrid containers use `autoHeight` + `minHeight` to avoid the `0px height` warning.

## Run
```bash
npm install
npm run dev
```

## Template
`public/JLPT_N2_Complete_Study_Plan.xlsx`
"# jlpt-study-plan" 
